<%= form_for(race_player) do |f| %>
  <% if race_player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(race_player.errors.count, "error") %> prohibited this race_player from being saved:</h2>

      <ul>
      <% race_player.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
